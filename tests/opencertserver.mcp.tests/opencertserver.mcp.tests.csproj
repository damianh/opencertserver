<Project Sdk="Microsoft.NET.Sdk">

     <PropertyGroup>
          <IsPackable>false</IsPackable>
          <RootNamespace>OpenCertServer.Mcp.Tests</RootNamespace>
          <!-- Test projects don't need trim/AOT compatibility -->
          <IsTrimmable>false</IsTrimmable>
          <IsAotCompatible>false</IsAotCompatible>
          <!-- Disable test parallelization: TestSharedState uses static mutable state, so scenarios must not run concurrently -->
          <CollectionBehavior>DisableTestParallelization</CollectionBehavior>
         <OutputType>exe</OutputType>
      </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Reqnroll.xUnit.v3"/>
        <PackageReference Include="xunit.v3.mtp-v2"/>
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\..\src\opencertserver.mcp\opencertserver.mcp.csproj"/>
        <ProjectReference Include="..\..\src\opencertserver.ca\opencertserver.ca.csproj"/>
        <ProjectReference Include="..\..\src\opencertserver.ca.utils\opencertserver.ca.utils.csproj"/>
        <ProjectReference Include="..\..\src\opencertserver.ca.server\opencertserver.ca.server.csproj"/>
    </ItemGroup>

</Project>
